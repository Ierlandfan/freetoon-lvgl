# build artefacts
build/
*.o
*.d
*.a
*.so

# cross-compiled ARM binaries (rebuild via make / build_*.sh on host)
quby_bridge/quby_bridge
quby_bridge/qubytap
p1bridge/p1bridge

# quby_bridge debug / capture artefacts
quby_bridge/*.cap
quby_bridge/*.dbg
quby_bridge/*.shim

# vendor toolchain & blobs (kept out of git — huge, downloadable elsewhere)
linaro/
*.tar.xz
*.tar.gz

# misc
*.swp
*~
.DS_Store

# python
__pycache__/
*.pyc
build-*/
